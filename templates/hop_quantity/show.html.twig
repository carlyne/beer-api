{% extends 'base.html.twig' %}

{% block title %}HopQuantity{% endblock %}

{% block body %}
    <h1>HopQuantity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hop_quantity.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ hop_quantity.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hop_quantity_index') }}">back to list</a>

    <a href="{{ path('hop_quantity_edit', {'id': hop_quantity.id}) }}">edit</a>

    {{ include('hop_quantity/_delete_form.html.twig') }}
{% endblock %}
